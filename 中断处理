中断处理：
硬件设备（如网卡、硬盘）需要CPU立即处理某些事件时，会发出中断信号。CPU会暂停当前任务，转去执行中断处理程序。

测试中的应用：中断会打断压力测试进程，引入性能抖动和延迟。

irqbalance服务：用于在各个CPU核心之间自动均衡中断处理，避免某个核心被中断淹没。但在性能测试中，我们通常会关闭它。

最佳实践： 
将压力测试进程绑定到一些核心上（如核心0-15），
然后将所有设备中断（使用 /proc/interrupts 查看，
使用 smp_affinity 设置）定向到其他不用于测试的核心上（如核心16-31），
从而实现中断隔离，获得最稳定、最纯净的测试结果。
